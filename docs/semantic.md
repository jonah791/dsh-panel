# dsh-panel 语义文档 v0.1 — 面板宿主（Panel Host）

> 状态：**草案 · 待主人评审** · 2026-09-12
> 开发模式：**语义文档优先**——先写下"它应当是什么"，再让实现逼近本文档，最后由实践反过来修改本文档。
> 本文档与被约束的代码同仓（`self-plugins/dsh-panel/docs/semantic.md`），随 GitHub 版本化。

---

## 0 · 文档自身的协议（这份文档怎么被修改）

1. **每条断言带标记**：`[MUST]` 硬性契约 / `[SHOULD]` 默认应遵守，例外需说明 / `[待逼近]` 尚未实现或尚未验证 / `[待定]` 需主人裁决。
2. **未验证不得写成已实现**：任何"已支持/已验证"的表述必须能在下节「§9 可证伪验收」里找到对应判据与实际证据（命令 + 输出摘录）。
3. **实践回写规则**：实现中若发现文档与现实冲突，**先记录冲突**（本节下方「偏离日志」），再决定改代码还是改文档；不许静默走偏。
4. **版本**：文档版本 = 插件 minor 版本锚（v0.1 文档 ↔ dsh-panel 0.2.x）。骨架变化（概念/契约增删）升 minor，措辞与示例修补只改日期。

### 偏离日志（实现 ↔ 文档冲突登记）

| 日期 | 文档条目 | 实际发现 | 处置 |
|---|---|---|---|
| 2026-09-12 | — | 初稿，尚无实现 | — |
| 2026-09-12 | §10 Q1–Q6 | 主人放开了沙箱与审批（自主执行授权），未逐条裁决六问 | **按文档建议默认执行且标注为"临时默认"**，待主人复核后修订；改动均可逆（Q1 升级现有仓库 = 可回退 tag；Q3 尚未实施；Q5 默认关闭；Q6 拒绝派发只收紧不放宽） |

---

## 1 · 定位（一句话 + 负边界）

**一句话**：dsh-panel 是**我的前端宿主**——所有自研插件需要给主人看/操作的东西，都通过它呈现；它提供一个 URL、一套外壳、一张注册表，把"每个插件各写一套前端"收敛成"每个插件提交一份声明"。

**它是什么**
- 一个**宿主**：托管页面、渲染视图、派发动作、记录审计、兜住错误。
- 一个**契约**：`ctx.panel` 服务 + 视图规格（ViewSpec）+ 动作 ABI（Action ABI）。
- 一个**唯一入口**：`http://127.0.0.1:3080/panel/`。

**它不是什么（负边界，同等重要）**
- ❌ 不是官方 Web GUI 的替代品：对话/会话仍是官方界面，面板只管"非对话类的可视化与操作"。
- ❌ 不是沙箱：贡献方与本宿主**同进程**，享有完整权限；见 §5 的能力诚实声明。
- ❌ 不是插件的业务逻辑：宿主不认识"磁盘清理""WQ 因子""任务板"——它只认 §4.2 的视图规格与 §4.3 的动作。
- ❌ 不是权限后门：任何触及"删数据 / 动凭据 / 动核心引擎"的动作，宿主**不得**成为绕过主体性铁律的通道（见 §5.4）。

---

## 2 · 术语（语义先于实现）

| 术语 | 语义 | 谁拥有 |
|---|---|---|
| **宿主（Host）** | 面板服务本体（dsh-panel）：外壳、路由、注册表、渲染、审计、错误隔离 | 本插件 |
| **贡献（Contribution）** | 一个插件向宿主提交的一份面板声明（身份 + 取数 + 动作），随该插件 apply 时注册、卸载时消失 | 消费方插件 |
| **面板（Panel）** | 宿主中一个可导航的条目，由一份贡献生成 | 宿主呈现，贡献方供数据 |
| **视图规格（ViewSpec）** | 一份 JSON 可序列化的界面描述（§4.2）——**宿主唯一能渲染的东西** | 贡献方生成，宿主编译 |
| **动作（Action）** | 贡献方声明的可执行操作（§4.3），由宿主派发并审计 | 贡献方实现，宿主派发 |
| **宿主自检面板** | 内置面板 #0：显示宿主自身与所有贡献的健康度（实测，不采信自报） | 宿主 |
| **消费方（Consumer）** | 任何 `inject: ['panel']` 并调用 `ctx.panel.register(...)` 的插件 | 各插件 |

> 语义纪律：**面板是"声明"，不是"页面"**。贡献方声明"我想让人看到什么、能操作什么"；至于长什么样（布局/配色/交互）由宿主统一决定——这是"一次写好，处处一致"的前提，也是主人不必学 N 种界面的原因。

---

## 3 · 概念模型

```
              ┌────────────────────────── 宿 主（dsh-panel）──────────────────────────┐
              │  GET /panel/            外壳 HTML（独立资源文件，零 client 依赖）      │
              │  ┌─ 导航（注册表投影）                                                │
  主人 ──────▶│  ├─ 面板渲染器（ViewSpec → DOM，白名单块类型）                        │
  (浏览器)     │  ├─ 动作派发器（Action ABI + 审计 + 破坏性二次确认）                  │
              │  ├─ 错误隔离（单面板失败不影响整壳；超时/降级可见）                    │
              │  └─ 自检面板 #0（宿主与各贡献的实测健康度）                            │
              └───────▲───────────────────────────────▲───────────────────────────┘
                      │ ctx.panel.register(contribution)│ view() / action()
              ┌───────┴────────┐              ┌───────┴────────┐
              │ 消费方插件 A    │              │ 消费方插件 B    │   …（同进程，effect 生命周期）
              │（如插件管理）   │              │（如成长档案）   │
              └────────────────┘              └────────────────┘
```

**权威划分（ambiguity killer）**
- 数据与语义权威：消费方（它知道自己的领域）。
- 呈现与一致性权威：宿主（它决定长的样子）。**贡献方不得指定花哨布局**，只能选白名单块类型。
- 健康与"自述 vs 实证"权威：宿主实测（§4.5）——贡献方对自己的健康**没有发言权**。

---

## 4 · 契约

### 4.1 注册契约 `[MUST]`

- 服务名：`ctx.panel`（Cordis Service，`class PanelHost extends Service`，`super(ctx, 'panel')`）。
- 消费方声明 `export const inject = ['panel']`；宿主未挂载时**必须**保持可用（§9-7）。
- **接入方式 `[MUST]`（2026-09-12 实践补入，见 §11.5）**：消费方**必须**用 `ctx.inject(['panel'], (panelCtx) => …)` 等待宿主，**不得**用 `ctx.get('panel')` 一次性探测——加载顺序（growth-profile 在 patch L98、dsh-panel 在 L224）会让 `get` 拿到 `undefined` 且**永不重试**，表现为「插件健康但面板 404/未知面板」。
- 接口（草案签名，实现时以类型为准）：

```ts
ctx.panel.register({
  id: 'plugin-manager',        // 全局唯一，[a-z0-9-]+；重复注册 fail-loud
  title: '插件管理',
  order?: 100,                 // 导航排序（小→前）
  icon?: 'plug',               // 宿主图标白名单键，非自由 SVG
  view: async (params) => ViewSpec,          // 取数（可 async；宿主施加超时）
  actions?: Record<string, ActionSpec>,      // 见 §4.3
  description?: string,                      // 一句话用途（自检面板展示）
}): () => void                 // 返回 disposer；宿主内部用 ctx.effect 托管
```

- **生命周期语义**：注册即 effect——插件卸载/禁用 → 面板立刻从注册表消失（无幽灵）。宿主不持久化贡献方数据。
- **重复 id**：第二个注册 `[MUST]` 抛错（fail-loud），不得静默覆盖。理由：面板身份是合成级契约，撞车=配置错误。

### 4.2 视图规格 `[MUST]`（宿主唯一渲染对象）

- 纯 JSON 可序列化（无函数、无 DOM、无 HTML 字符串）——保证"宿主能渲染、日志能落盘、测试能快照"。
- v0.1 白名单块（**先小后扩，扩展走文档修订**）：

| block | 语义 | 关键字段 |
|---|---|---|
| `metrics` | 关键指标卡 | `items: [{label, value, hint?, tone?: ok\|warn\|bad\|muted}]` |
| `table` | 表格（可带行内动作） | `columns: [{key,label,align?}]`, `rows: object[]`, `rowActions?: actionId[]` |
| `list` | 卡片列表 | `items: [{title, subtitle?, tags?: string[], tone?}]` |
| `kv` | 键值明细 | `pairs: [{key, value}]` |
| `text` | 说明段落（纯文本，宿主负责转义） | `lines: string[]` |
| `timeline` | 事件序列 | `events: [{at, title, detail?, tone?}]` |
| `actions` | 动作按钮组 | `items: actionId[]` |
| `sections` | 容器（唯一嵌套块） | `title?, blocks: Block[]` |

- `[SHOULD]` 每个块带可选 `title`；空数据渲染为空态文案，不渲染空白区域。
- `[MUST]` 宿主对所有字符串做 HTML 转义；**ViewSpec 里出现原始 HTML 一律当文本**。

### 4.3 动作 ABI `[MUST]`

```ts
type ActionSpec = {
  label: string
  level: 'read' | 'write' | 'destructive'
  params?: Record<string, 'string' | 'number' | 'boolean'>   // 宿主据此生成最小表单
  run: (params, ctx: { sessionId?: string; now: string }) => Promise<{
    ok: boolean
    data?: unknown      // 必须是可序列化；宿主渲染为 JSON 文本块
    message?: string    // 面向主人的一句结果说明
  }>
}
```

- 派发：`POST /api/panel/action` body `{panelId, actionId, params, confirm?}`。
- `[MUST]` 未知 `panelId`/`actionId` → 400/404，**零副作用**，写审计。
- `[MUST]` `level: 'destructive'` 且 `confirm !== true` → 拒绝执行 + 审计（`denied`）。
- `[MUST]` 每次派发写审计：`DSH_HOME/panel/audit.jsonl`，字段 `{at, panelId, actionId, level, paramsDigest, outcome, durationMs, message}`——`paramsDigest` 存哈希不存原值（凭据不落盘纪律）。
- `[SHOULD]` 动作执行超时（默认 10s）→ 返回 `ok:false, message:'超时'`，副作用由贡献方自行负责幂等。

### 4.4 HTTP 面 `[MUST]`

| 方法 | 路径 | 语义 |
|---|---|---|
| GET | `/panel/` | 外壳 HTML（独立资源文件；`cache-control: no-store`） |
| GET | `/panel/assets/*` | 宿主静态资源（prefix 路由，白名单目录，禁路径穿越） |
| GET | `/api/panel/registry` | 注册表投影：`[{id,title,order,icon,description,health}]`（**不含** ViewSpec） |
| GET | `/api/panel/view?id=` | `{ok, spec, generatedAt, durationMs, degraded?}` |
| POST | `/api/panel/action` | §4.3 派发 |
| GET | `/api/panel/selfcheck` | 宿主自检：贡献健康实测 + 宿主版本 + 计时统计 |

- `[MUST]` 全部 JSON 响应统一信封 `{ok: boolean, ...}`；错误含 `error` 字段（字符串）。
- `[待逼近]` 实时性：v0.1 = 轮询（面板可见时 5s，不可见时停）+ 手动刷新；SSE 见 §10-Q4。

### 4.5 失效与降级语义 `[MUST]`（本次事故的纪律下沉）

- **单面板故障隔离**：`view()` 抛错 → 该面板渲染降级态（显示错误原文 + 重试按钮），**其余面板与外壳不受影响**。
- **超时**：`view()` 超过 `viewTimeoutMs`（默认 2000ms）→ 该面板标记 `超时`，宿主返回 `degraded: true` 而不是一直转圈。
- **实测健康，不采信自报**：宿主记录每次 `view()` 的 `ok/durationMs`，据最近 N 次得出 `health: ok|degraded|down`。**贡献方声明的状态文字不得作为健康依据**（§5.0 的纪律：机制自述 ≠ 实证；见 AGENTS.md §5.17）。
- **宿主缺位**：`ctx.panel` 不存在时，消费方 `[MUST]` 降级（记录日志/继续原有行为），不得抛错、不得阻塞自身主功能。
- **宿主自身不可用**：面板页 404/500 时，主人仍可用 Telegram 与对话通道——面板是**增强**，不是命脉。

---

## 5 · 信任与安全边界

### 5.0 能力诚实声明（能力 ≠ 沙箱）
面板宿主提供的是**便利与一致性**，不是隔离。贡献方与本插件同进程运行，拥有与我相同的权限——这在自研插件生态里是可接受的（都是"我"），但**必须写在 README 与文档里**，不得宣传为安全边界。

### 5.1 网络面 `[MUST]`
- 仅监听回环（`host=127.0.0.1`）；页面与 API 同源，认证由宿主 cookie 承担；`/api/*` 不额外造 token 体系（同源信任模型，沿用 `dsh-panel-plugin` 技能既有结论）。
- `[MUST]` 路径穿越防护（assets 白名单 + `resolve` 前缀校验，参照官方 `frontend-static` 的 `sep` 用法，Windows 反斜杠是已知坑）。
- `[待定]` 公网暴露（`0.0.0.0`）时面板必须强制关闭或加独立认证——默认不提供。

### 5.2 破坏性动作 `[MUST]`
- 三层分级（`read/write/destructive`）+ 二次确认 + 审计。
- 不可逆操作（删除数据、卸载带数据的插件）**默认不提供面板入口**；确需时必须走 §5.4。

### 5.3 输入 `[MUST]`
- 全部动作参数经类型 + 白名单校验（`params` schema 由贡献方声明，宿主校验后才调用 `run`）。
- `paramsDigest` 落盘用哈希；原值不入审计日志。

### 5.4 与主体性铁律的关系 `[MUST]`
面板**不得**成为绕过 AGENTS.md「须请示三类（删数据 / 动凭据 / 动核心引擎）」的后门：
- 这类动作在宿主侧**拒绝派发**，改为产出"请示项"（写任务板/Telegram 提请），由我在正常通道拿到授权后再执行。
- 判据（可测试）：注入一个 `destructive` 且触及"删数据"的动作 → 宿主返回 `denied` + 审计 `reason='requires-approval'`，且**未调用** `run`。

---

## 6 · 与官方 Web GUI 的关系（三条通道的取舍）

| 通道 | 机制 | 优点 | 代价 | 结论 |
|---|---|---|---|---|
| **独立页** | `webServer.register('/panel/')` + 自包含 HTML | 零官方 client 依赖；官方升级不易碎 | 与对话界面分离，需要单独切换/开标签 | **主通道** |
| **官方 index 注入** | `webserver/index-inject` 结构化行（`global/script/script-src/style/html`） | 可在官方页面加入口/徽标/样式，主人不必记 URL | 依赖官方 DOM/样式约定，有升级耦合 | `[待定]`：M3 评估，先做"轻量导航入口"而不是深度嵌 UI |
| **官方 client slot / Conversation Node** | 官方 client bundle（08-16 成长档案 tab 先例） | 与对话同屏，体验最佳 | bundle + 版本对齐成本（09-11 审计已量化） | **不采用**（保持"面板不跟官方版本纠缠"的初衷） |

---

## 7 · 非功能约束 `[MUST]`

1. 零 `@deepseek-ai/dsh-client-*` 依赖（构建产物可 grep 断言）。
2. HTML/JS 资源放**独立文件**（`src/assets/*` → 构建拷 `lib/assets/`），**永不写进 TS 模板字符串**（`dsh-panel-plugin` 技能铁律，历史踩坑）。
3. 不引重型前端依赖（无框架、无 CDN、无外部字体）；外壳 ≤ 60KB。
4. 无外部网络请求（面板永不访问公网）。
5. `view()` 取数应为只读快照；写操作一律走 `actions`。
6. 命名遵循官方 `plugin-write` 规范（`dsh-plugin.naming.json` 各 surface 名一致，routes 不以 `/` 结尾）。

---

## 8 · 迁移路径（现存四处自研前端 → 宿主）

现状盘点（2026-09-12）：`dsh-panel`（`/panel/` + `/api/panel/*`）、`dsh-growth-profile`（`/api/growth-profile`）、`dsh-agent-browser`（`/browser`）、`dsh-agent-teams`（`/plugins/dsh-agent-teams/{state,halt,plan,assets}`）——**四种路径约定、四套自写界面**。

| 里程碑 | 内容 | 完成判据 |
|---|---|---|
| **M1** | 宿主内核（服务 + 路由 + 渲染器 + 动作派发 + 自检面板）；**插件管理面板迁为 Panel #1**（吃自己的狗粮）；契约单测 + 尸体测试 | §9-1~9-9 全过；`/panel/` 可浏览插件管理且行为与旧版等价 |
| **M2** | 迁入 growth-profile（成长档案）、taskboard（任务板）、teams（成员状态）为声明式面板 | 各旧路由保留一个版本周期（兼容），新面板数据与旧页一致（对照验收） |
| **M3** | 官方 index 轻量导航入口；SSE 或增量刷新；面板搜索/置顶；`ctx.panel` 服务对外发布 + README/命名核对 + 可选 release | 主人可从官方界面一键进入面板；贡献方接入只需 ≤ 30 行 |

`[待定]` 关于 `plugin-inventory`：官方已提供只读插件投影（Remote-only，无同进程 Cordis 合并）。M1 的插件管理面板**先保留自扫实现**（它需要挂载/启停等写操作，官方服务只读且走 RPC）；M2 评估是否用官方投影替换"读取"部分以降低维护面。

---

## 9 · 可证伪验收（每条都要能"用坏样本证明它真的会拦"）

| # | 判据 | 证据形式 |
|---|---|---|
| 1 | 卸载任一贡献插件 → 重启后面板消失，`/api/panel/registry` 无残留 | registry 快照前后对比 |
| 2 | 贡献 `view()` 抛错 → 该面板降级显示错误原文，其余面板与外壳正常 | 注入坏贡献（尸体测试）→ 截图/DOM 断言 |
| 3 | 贡献 `view()` 挂起 > 超时 → 标记超时，外壳与其它面板不受阻 | 坏样本 + 实测耗时 |
| 4 | 重复 `panelId` 注册 → fail-loud（抛错），不静默覆盖 | 单测断言 |
| 5 | 未知 `panelId/actionId` → 400/404 且零副作用 + 审计一条 | 单测 + audit.jsonl |
| 6 | `destructive` 无 `confirm` → 拒绝执行，审计 `denied` | 单测 |
| 7 | 触及"须请示三类"的动作 → 宿主拒绝派发（`requires-approval`），`run` 未被调用 | 单测 + 审计 |
| 8 | 宿主缺位时消费方不崩（降级日志） | 组合测试（去掉 panel 行） |
| 9 | 面板产物零官方 client 依赖、外壳体积达标、无公网请求 | 构建产物 grep + 体积 + 抓包/DOM 断言 |
| 10 | 自检面板的健康度来自宿主实测（喂"自报健康但实际超时"的贡献 → 判 degraded） | 坏样本注入 |

---

## 10 · 未决问题（请主人裁决，逐条给了我的建议）

| # | 问题 | 我的建议 |
|---|---|---|
| **Q1** | 命名与版本：把现有 `dsh-panel` 升级为 v0.2.0 宿主（保持仓库/URL/命名），还是新建 `dsh-panel-host`？ | **升级现有**——URL、仓库、GitHub 历史都可续用；插件管理面板顺势成为 Panel #1 |
| **Q2** | 强制程度：以后新插件的前端**必须**走宿主，还是自愿接入？ | **公约强制 + 显式例外**（例外需在该插件 README 写明理由），否则"统一入口"会随插件数量稀释 |
| **Q3** | 要不要在官方 GUI 里注入导航入口（index-inject）？ | **先不做，放到 M3 评估**——避免早期就与官方 DOM 耦合 |
| **Q4** | 实时性：轮询还是 SSE？ | **v0.1 轮询（5s，页面不可见即停）**；等面板数量与数据量上来再上 SSE |
| **Q5** | 逃生舱：允不允许贡献方给自定义 HTML 页？ | **允许但需显式标记 + 默认关闭 + 审计**（自定义页 = 绕过宿主一致性，应当可见） |
| **Q6** | 确认模型：破坏性动作"UI 二次确认 + `confirm:true`"够不够？触及须请示三类的动作是否一律由宿主拒绝并转请示通道？ | **一律拒绝 + 转请示**（§5.4）——宿主不替我做违反铁律的决定 |

---

## 11 · 逼近计划（文档 → 实现 → 回写）

1. **评审门**：主人对 §10 六问裁决 → 本文档升 v0.2（改动入「偏离日志」并注明裁决人）。
2. **M1 实现**：按 §4 契约落地宿主内核 + Panel #1；每完成一项在 §9 表内填证据。
3. **实践回写**：实现中每一次"文档说不通"的地方登记偏离日志，评审后改文档（这是本模式的核心产物：**文档因实践而精确**）。
4. **对外**：M3 完成后更新 README（定位/功能/安装/配置/技术要点/能力边界诚实声明）+ 走 `github-plugin-publish` 流程。

---

## 11.5 · 实践修订记录（M1/M2 首次实践 · 2026-09-12）

（本模式第三拍：**实践回修语义文档**。此节是 I3/D6 的检查对象——缺它即不合格。）

- **被实践确认**：宿主外壳**自包含**（`src/assets/shell.html`，48KB，1 个 script、0 个外部 URL）在 `webServer.register` 的普通 handler 下工作正常，线上实测 `/panel/` 200；三条 GUI 通道的取舍（独立页面为主）成立。
- **被实践修正（实现侧）**：初版消费方用 `ctx.get('panel')` 探测宿主 → 因加载顺序拿到 `undefined` 且永不重试。语义文档 §4.1 早已写「消费方声明 `inject`」，是实现**没对齐文档**——修正后（`ctx.inject(['panel'], …)`）线上验收通过：registry 3 面板、`/api/panel/view?id=growth-profile` → `ok=true · 7 块 · 116ms`、selfcheck `growth-profile: ok`。→ 已把「必须用 inject 等待、不得用 get 探测」提升为 §4.1 的 `[MUST]` 条款。
- **被实践补充（健康语义）**：`health` 由**宿主实测**（`ok` / `durationMs` / 错误摘要），消费方**不得自报**——M2 实测值 `116ms` 即为宿主实测；这条原文档只在动词上提过，现明确为「宿主是健康权威」。
- **被实践补充（调用点清单）**：文档原先只写「消费方注册面板」，实现时才发现接入方式有 3 种（inject 等待 / get 探测 / 直接 import），只有 1 种正确。→ 教训与 web 生命周期文档同源：**只写「谁做什么」不够，必须写「在哪些调用点生效」**（已回写技能 `semantic-doc-first` 反模式 ②）。
- **被实践确认（破坏性动作守卫）**：宿主对触及「须请示三类」的动作一律拒绝并转审批通道——M1 实现中未出现绕行需求。

---

## 11.6 · 与实现的关系

- **主实现**（本文档描述的唯一真源）：`src/index.ts`（插件装配 + webServer 注册）、`src/host.ts`（`PanelHost` Service：注册表/路由/超时/审计）、`src/registry.ts`（注册与 ViewSpec 校验）、`src/types.ts`（契约类型）、`src/audit.ts`（`audit.jsonl` + paramsDigest）、`src/assets/shell.html`（自包含外壳，零官方 client 依赖）。
- **消费方实现**（非副本，各自独立）：`self-plugins/dsh-growth-profile/src/panel.ts`（首个消费方；不 import 宿主类型，用本地结构类型 + `ctx.inject(['panel'])` 接入）。
- **同语义副本**：无——本能力单实现单仓库；跨插件协作走**契约**（§4.1/§4.2/§4.3）而非复制源码。
- **未实现 / 未验证（显式标注）**：M3 的官方 `index-inject` 导航入口（未做，见 §6）；SSE 实时推送（v0.1 走轮询，见 U）；`ctx.panel` 的对外发布与命名规范（未做）。
- **系统注册**：本文档登记于语义文档系统 `docs/semantics/registry.json`（id=`panel-host`），drift 检查（D1–D6）覆盖本文件。

---

## 12 · 变更史

| 版本 | 日期 | 变化 |
|---|---|---|
| v0.1 | 2026-09-12 | 初稿：定位/术语/概念模型/契约（注册·视图规格·动作 ABI·HTTP 面·失效语义）/信任边界/三条 GUI 通道取舍/迁移 M1-M3/10 条可证伪验收/6 个未决问题 |
| v0.2 | 2026-09-12 | **实践回修（第三拍）**：§4.1 新增接入方式 `[MUST]`（inject 等待 > get 探测）；新增 §11.5 实践修订记录（4 条：被确认/被修正/被补充×2）；新增 §11.6 与实现的关系（补系统必备节，修 D4）；登记进语义文档系统 `docs/semantics/registry.json`（id=panel-host） |
